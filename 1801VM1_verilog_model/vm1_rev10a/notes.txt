~plm23_0 - высокий уровень запускает опрос матрицы PLI
plm12r (plm_ena & pl12) генерирует ошибку Undefined Opcode
ERR7 (бит 7 регистра ошибки) (прерывание не генерирует. только флаг)
ERR3 (бит 3 регистра ошибки) (прерывание не генерирует - оторвано)
ERR2 (бит 3 регистра ошибки) (прерывание не генерирует - оторвано)

plr18r - бит словности аргумента (16/8)

